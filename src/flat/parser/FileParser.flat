package flat/parser

import flat/ast/FileNode

class extends Parser {
  override let lazy Parser[] childParsers => Array<Parser>():addAll([
    ImportParser(),
    PackageParser(),
    ClassParser()
  ])

  override public async generateNode(ParseContext context, TokenMatch match) -> FileNode {
    return null
  }
}