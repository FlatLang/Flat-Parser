package flat/parser

import flat/ast/CommentNode
import flat/compiler/models/Token

class extends ParserBase {
  override let lazy TokenMatcher matcher => TokenMatcherGroup([
    TokenMatcher(Token.Type.COMMENT, label: "comment")
  ])

  override public generateNode(ParseContext context, TokenMatch match) -> CommentNode {
    let value = match.getValue("comment")

    return CommentNode(
      value: value
    )
  }
}